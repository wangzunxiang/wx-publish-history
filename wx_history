#!/usr/bin/env bash
# 公众号发布历史查询工具 启动脚本
DIR="$(cd "$(dirname "$0")" && pwd)"
VENV="$DIR/.venv"
PORT="${1:-8765}"
# 优先用本目录 venv（含 playwright），否则系统 python
if [ -x "$VENV/bin/python" ]; then
  PY="$VENV/bin/python"
else
  PY="$(command -v python3)"
fi
exec "$PY" "$DIR/wx_history.py" "$PORT"
